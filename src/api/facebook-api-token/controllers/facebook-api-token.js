'use strict';

/**
 *  facebook-api-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::facebook-api-token.facebook-api-token');

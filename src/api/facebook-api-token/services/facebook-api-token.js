'use strict';

/**
 * facebook-api-token service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::facebook-api-token.facebook-api-token');
